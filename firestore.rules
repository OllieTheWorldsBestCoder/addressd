rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /addresses/{addressId} {
      allow read, write: if true;  // For testing only - we'll make this more secure later
    }
  }
}